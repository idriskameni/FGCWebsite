// Exporting multiple components from a single module
export { 
    default as Map, // Export Map component with the name "Map"
    default as Header, // Export Header component with the name "Header"
    default as Footer, // Export Footer component with the name "Footer"
    default as LineSelector, // Export LineSelector component with the name "LineSelector"
    default as AppButton, // Export AppButton component with the name "AppButton"
    default as TrainPositionPopup, // Export TrainPositionPopup component with the name "TrainPositionPopup"
    default as TrainPosition, // Export TrainPosition component with the name "TrainPosition"
    default as Legend, // Export Legend component with the name "Legend"
    default as ModelAlert // Export ModelAlert component with the name "ModelAlert"
  };
  